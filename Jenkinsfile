pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                        // Example of a Docker command
                        sh 'docker-compose up -d'
                    }
                }
            }
        }
        
