pipeline {
    agent any
    }

    stages {
        stage('Start Containers') {
            steps {
                
                    sh 'docker-compose up -d'
                } 
            }
        }